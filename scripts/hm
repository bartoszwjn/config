#!/usr/bin/env bash
# Run home-manager for the current home configuration

set -euo pipefail

user=$(id -un)
host=$(uname -n)

PS4="+> "
set -x

nix run "$LINUX_CONFIG_ROOT#home-manager" -- --flake "$LINUX_CONFIG_ROOT#$user@$host" "$@"
